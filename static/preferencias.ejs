<%- contentFor('css') %>

<title>Preferencias</title>

<%- contentFor('js') %>

<%- contentFor('content') %>

<% include ./header %>

<h2>Preferencias</h2>

<% if(!data.length){ %>

<h6>No hay preferencias todavía. Esperemos un poco mas!</h6>

<% }else{ %>

<ul>
    <% data.forEach(function(flet) { %>
        <li><%= flet.datos.nombre %> - <%= flet.ruta.distance.text %></li>
    <% }); %>
</ul>

<% } %>
